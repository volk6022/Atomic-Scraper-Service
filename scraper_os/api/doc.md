# API Presentation Layer

This directory contains the HTTP and WebSocket interfaces for the Scraper OS.

- `routers/`: REST endpoints for stateless tasks and session management.
- `websockets/`: WebSocket bridge for stateful session interaction.
- `main.py`: Application entry point and router registration.
